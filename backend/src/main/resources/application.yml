spring:
  application:
    name: nexttick
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    open-in-view: false

  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/testdb
    username:
    password: